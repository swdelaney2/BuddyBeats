<h1>REMEMBER THIS PAGE IS NOT BEING USED</h1>

<h1>Songs#new</h1>
<p>
<h1>Add song</h1>

<%= form_for @song do |inc| %>


<%= inc.label :title %>
<%= inc.text_field :title %>

<%= inc.label :youtube_id %>
<%= inc.text_field :youtube_id %>

<%= inc.hidden_field :playlist_id, :value => params[:playlist_id] %>

<%= inc.hidden_field :playlist_hex, :value => params[:random_hex] %>

<%= inc.hidden_field :account_id, :value => @current_account.id %>



<%= inc.submit %>


<% end %>

<p><hr><p>
can also do this:
form_for @account, html: {class: "whatever"} do |inc|
