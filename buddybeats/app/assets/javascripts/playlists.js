// Place all the behaviors and hooks related to the matching controller here.
// All this logic will automatically be available in application.js.

// This creates the tooltip that is used when a user hovers over "quantity" to understand what it is. Later, I realized I could have used a tooltip that comes with Bootstrap, but this is good, too!
$(function() {
  $( document ).tooltip();
});
